"use client";

import { CreateProgramScreen } from "@ihp/enterprise-trainings";

/** Renders the Program editor, which explains any backend-required Enterprise dependency in context. */
export default function CreateEnterpriseProgramPage() {
  return <CreateProgramScreen />;
}