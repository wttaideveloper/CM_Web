"use client";

import { EditEventScreen } from "@ihp/enterprise-events";

/** Renders the Event editor, with tenant ownership enforced by the backend. */
export default function EnterpriseEventEditPage() {
  return <EditEventScreen />;
}
