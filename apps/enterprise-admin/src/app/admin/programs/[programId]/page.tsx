"use client";

import { ProgramDetailsScreen } from "@ihp/enterprise-trainings";

/** Renders a Program detail workspace authorized by the backend's current tenant session. */
export default function EnterpriseProgramDetailsPage() {
  return <ProgramDetailsScreen />;
}