"use client";

import { EditTrainingScreen } from "@ihp/enterprise-trainings";

/** Renders the Training editor, with tenant ownership enforced by the backend. */
export default function EnterpriseTrainingEditPage() {
  return <EditTrainingScreen />;
}