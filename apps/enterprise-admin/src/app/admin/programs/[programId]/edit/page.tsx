"use client";

import { EditProgramScreen } from "@ihp/enterprise-trainings";

/** Renders the Program editor, with tenant ownership enforced by the backend. */
export default function EnterpriseProgramEditPage() {
  return <EditProgramScreen />;
}