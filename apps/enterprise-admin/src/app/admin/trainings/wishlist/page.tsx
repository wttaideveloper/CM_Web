"use client";

import { WishlistScreen } from "@ihp/enterprise-trainings";

/** Participant wishlist (local shell until a backend endpoint ships). */
export default function EnterpriseTrainingWishlistPage() {
  return <WishlistScreen />;
}