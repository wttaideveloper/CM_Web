"use client";

import { ParticipantEnrolmentsScreen } from "@ihp/enterprise-trainings";

/** Participant dashboard — enrolled, active, completed, and cancelled trainings. */
export default function EnterpriseMyEnrolmentsPage() {
  return <ParticipantEnrolmentsScreen />;
}