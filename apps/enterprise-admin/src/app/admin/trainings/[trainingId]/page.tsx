"use client";

import { TrainingDetailsScreen } from "@ihp/enterprise-trainings";

/** Renders a Training detail workspace authorized by the backend's current tenant session. */
export default function EnterpriseTrainingDetailsPage() {
  return <TrainingDetailsScreen />;
}