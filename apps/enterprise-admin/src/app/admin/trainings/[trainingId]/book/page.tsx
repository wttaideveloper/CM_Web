"use client";

import { TrainingBookingFlow } from "@ihp/enterprise-trainings";

/** Learner booking flow: review → details → confirm → done. */
export default function EnterpriseTrainingBookingPage() {
  return <TrainingBookingFlow />;
}
