"use client";

import { CreateEventScreen } from "@ihp/enterprise-events";

/** Renders the Event editor, which explains any backend-required Enterprise dependency in context. */
export default function CreateEnterpriseEventPage() {
  return <CreateEventScreen />;
}
