"use client";

import { CreateTrainingScreen } from "@ihp/enterprise-trainings";

/** Renders the Training editor, which explains any backend-required Enterprise dependency in context. */
export default function CreateEnterpriseTrainingPage() {
  return <CreateTrainingScreen />;
}