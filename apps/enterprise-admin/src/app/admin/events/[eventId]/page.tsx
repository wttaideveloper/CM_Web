"use client";

import { EventDetailsScreen } from "@ihp/enterprise-events";

/** Renders an Event detail workspace authorized by the backend's current tenant session. */
export default function EnterpriseEventDetailsPage() {
  return <EventDetailsScreen />;
}
