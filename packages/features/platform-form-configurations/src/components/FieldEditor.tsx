"use client";

import { formConfigurationCopy as copy } from "../constants/form-configuration-copy";
import { getEventCompositeFieldDefinition } from "../model/event-composite-field-definitions";
import { normalizeOptionPositions } from "../model/form-configuration-ordering";
import type { ConfiguredField, CoreFieldRegistryItem, FormFieldOption, FormFieldValidation, FormRenderer } from "../model/form-configuration.types";
import { CompositeFieldEditor } from "./CompositeFieldEditor";
const renderers = Object.keys(copy.fieldTypes) as FormRenderer[];

/** Edits a selected field while preserving registry restrictions and builder identities. */
export function FieldEditor({ field, registry, validationErrors = [], onChange, onDelete }: { field: ConfiguredField; registry: readonly CoreFieldRegistryItem[]; validationErrors?: readonly string[]; onChange: (patch: Partial<ConfiguredField>) => void; onDelete: () => void }) {
  const core = field.coreKey ? registry.find((item) => item.key === field.coreKey) : undefined; const compositeDefinition = field.source === "core" ? getEventCompositeFieldDefinition(field.coreKey) : undefined; const allowedRenderers = core?.allowedRenderers ?? renderers; const rendererCanChange = !core || (core.configurable.renderer && core.allowedRenderers.length > 1); const textValidation = field.valueType === "string"; const numberValidation = field.valueType === "number";
  const updateOption = (index: number, patch: Partial<FormFieldOption>) => onChange({ options: normalizeOptionPositions(field.options.map((option, optionIndex) => optionIndex === index ? { ...option, ...patch } : option)) });
  const moveOption = (index: number, amount: number) => { const target = index + amount; if (target < 0 || target >= field.options.length) return; const options = [...field.options]; [options[index], options[target]] = [options[target], options[index]]; onChange({ options: normalizeOptionPositions(options) }); };
  const setValidation = (patch: Partial<FormFieldValidation>) => onChange({ validation: { ...field.validation, ...patch } });
  return <article className="rounded-xl border border-[#dfe9e4] bg-white p-4"><div className="flex flex-wrap items-center justify-between gap-3"><p className="font-bold text-[#06201c]">{field.label} <span className="ml-2 rounded-full bg-[#eef6f2] px-2 py-0.5 text-xs text-[#1f6a58]">{field.source === "core" ? copy.core : copy.custom}</span></p>{core && !core.removable ? <span className="text-xs text-[#52736a]">{copy.immutable}</span> : <button type="button" onClick={onDelete} className="text-sm font-semibold text-[#b42318]">{copy.delete}</button>}</div>{validationErrors.length ? <div role="alert" className="mt-3 rounded-lg border border-[#f0c8c4] bg-[#fff8f7] p-3 text-sm font-semibold text-[#b42318]">{validationErrors.map((message) => <p key={message}>{message}</p>)}</div> : null}{core ? <div className="mt-3 rounded-lg bg-[#f4faf7] p-3 text-xs text-[#52736a]"><p>{copy.coreKey}: <code>{core.key}</code></p><p className="mt-1">{copy.valueType}: <code>{field.valueType}</code></p>{core.requiredByDomain ? <p className="mt-1">{copy.domainRequired}</p> : null}{!rendererCanChange ? <p className="mt-1">{copy.rendererLocked}</p> : null}<p className="mt-1">{copy.coreKeyLocked}</p></div> : <p className="mt-3 text-xs text-[#52736a]">{copy.valueType}: <code>{field.valueType}</code></p>}{core && !core.allowedRenderers.includes(field.renderer) ? <button type="button" onClick={() => onChange({ renderer: core.defaultRenderer, valueType: core.valueType })} className="mt-3 rounded-full border border-[#cfe0d8] px-3 py-1.5 text-sm font-bold text-[#1f6a58]">Use registry renderer: {core.defaultRenderer}</button> : null}{core && core.requiredByDomain && !field.required ? <button type="button" onClick={() => onChange({ required: true })} className="mt-3 rounded-full border border-[#cfe0d8] px-3 py-1.5 text-sm font-bold text-[#1f6a58]">Mark required by Event domain</button> : null}{core && compositeDefinition ? <CompositeFieldEditor field={field} core={core} definition={compositeDefinition} onChange={onChange} /> : <><div className="mt-4 grid gap-3 sm:grid-cols-2">{!core || core.configurable.label ? <TextControl label={copy.label} value={field.label} onChange={(label) => onChange({ label })} /> : null}{rendererCanChange ? <SelectControl label={copy.renderer} value={field.renderer} onChange={(renderer) => onChange({ renderer: renderer as FormRenderer })} options={allowedRenderers.map((renderer) => [renderer, copy.fieldTypes[renderer as keyof typeof copy.fieldTypes] ?? renderer])} /> : null}{!core || core.configurable.placeholder ? <TextControl label={copy.placeholder} value={field.placeholder} onChange={(placeholder) => onChange({ placeholder })} /> : null}{!core || core.configurable.helpText ? <TextControl label={copy.helpText} value={field.helpText} onChange={(helpText) => onChange({ helpText })} /> : null}{!core || core.configurable.required ? <label className="flex items-center gap-2 pt-6 text-sm text-[#355a51]"><input type="checkbox" checked={field.required} disabled={Boolean(core?.requiredByDomain && field.required)} onChange={(event) => onChange({ required: core?.requiredByDomain ? true : event.target.checked })} />{copy.required}</label> : null}<label className="flex items-center gap-2 pt-6 text-sm text-[#355a51]"><input type="checkbox" checked={field.enabled} disabled={Boolean(core && !core.hideable)} onChange={(event) => onChange({ enabled: event.target.checked })} />{copy.enabled}</label></div>{(!core || core.configurable.validation) && textValidation ? <ValidationEditor validation={field.validation} onChange={setValidation} text /> : null}{(!core || core.configurable.validation) && numberValidation ? <ValidationEditor validation={field.validation} onChange={setValidation} /> : null}{(field.renderer === "select" || field.renderer === "multi_select") ? <OptionEditor options={field.options} onChange={(options) => onChange({ options: normalizeOptionPositions(options) })} onUpdate={updateOption} onMove={moveOption} /> : null}</>}</article>;
}
function ValidationEditor({ validation, onChange, text = false }: { validation: FormFieldValidation; onChange: (patch: Partial<FormFieldValidation>) => void; text?: boolean }) { return <fieldset className="mt-4 grid gap-3 rounded-xl border border-[#edf3f0] p-3 sm:grid-cols-2"><legend className="px-1 text-sm font-semibold text-[#355a51]">{copy.validation}</legend>{text ? <><NumberControl label={copy.minLength} value={validation.minLength} onChange={(minLength) => onChange({ minLength })} /><NumberControl label={copy.maxLength} value={validation.maxLength} onChange={(maxLength) => onChange({ maxLength })} /><TextControl label={copy.pattern} value={validation.pattern ?? ""} onChange={(pattern) => onChange({ pattern: pattern || null })} /></> : <><NumberControl label={copy.minimum} value={validation.min} onChange={(min) => onChange({ min })} /><NumberControl label={copy.maximum} value={validation.max} onChange={(max) => onChange({ max })} /></>}</fieldset>; }
function OptionEditor({ options, onChange, onUpdate, onMove }: { options: FormFieldOption[]; onChange: (options: FormFieldOption[]) => void; onUpdate: (index: number, patch: Partial<FormFieldOption>) => void; onMove: (index: number, amount: number) => void }) { return <fieldset className="mt-4 rounded-xl border border-[#edf3f0] p-3"><legend className="px-1 text-sm font-semibold text-[#355a51]">{copy.options}</legend><div className="space-y-2">{options.map((option, index) => <div key={`${option.value}-${index}`} className="grid grid-cols-[1fr_1fr_auto] gap-2"><input aria-label={copy.optionLabel} value={option.label} onChange={(event) => onUpdate(index, { label: event.target.value })} className="rounded-lg border border-[#cfe0d8] px-2 py-1.5 text-sm" /><input aria-label={copy.optionValue} value={option.value} onChange={(event) => onUpdate(index, { value: event.target.value })} className="rounded-lg border border-[#cfe0d8] px-2 py-1.5 text-sm" /><span className="flex gap-1"><button type="button" aria-label={copy.moveUp} onClick={() => onMove(index, -1)} className="px-1 text-[#1f6a58]">↑</button><button type="button" aria-label={copy.moveDown} onClick={() => onMove(index, 1)} className="px-1 text-[#1f6a58]">↓</button><button type="button" aria-label={copy.delete} onClick={() => onChange(options.filter((_, itemIndex) => itemIndex !== index))} className="px-1 text-[#b42318]">×</button></span></div>)}</div><button type="button" onClick={() => onChange([...options, { label: copy.newOption, value: "", position: options.length + 1 }])} className="mt-3 text-sm font-bold text-[#1f6a58]">{copy.addOption}</button></fieldset>; }
function TextControl({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="text-sm font-semibold text-[#355a51]"><span>{label}</span><input value={value} onChange={(event) => onChange(event.target.value)} className="mt-1.5 w-full rounded-lg border border-[#cfe0d8] px-3 py-2 font-normal" /></label>; }
function NumberControl({ label, value, onChange }: { label: string; value: number | null | undefined; onChange: (value: number | null) => void }) { return <label className="text-sm font-semibold text-[#355a51]"><span>{label}</span><input type="number" value={value ?? ""} onChange={(event) => onChange(event.target.value === "" ? null : Number(event.target.value))} className="mt-1.5 w-full rounded-lg border border-[#cfe0d8] px-3 py-2 font-normal" /></label>; }
function SelectControl({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: readonly (readonly [string, string])[] }) { return <label className="text-sm font-semibold text-[#355a51]"><span>{label}</span><select value={value} onChange={(event) => onChange(event.target.value)} className="mt-1.5 w-full rounded-lg border border-[#cfe0d8] bg-white px-3 py-2 font-normal">{options.map(([optionValue, optionLabel]) => <option key={optionValue} value={optionValue}>{optionLabel}</option>)}</select></label>; }
